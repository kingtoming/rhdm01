package com.axa.dm.autouw;

import java.lang.Number;

rule "mapClassCode - Policy (substandard)"
	dialect "mvel"
	when
		$policy : Policy( $planCode : planCode, $classCode : classCode, standardClass == null )
		StandardRateObject( planCode == $planCode )
		not (StandardRateObject( classCode == $classCode , planCode == $planCode )) 
	then
		modify( $policy ) {
				setStandardClass( false )
		}
end
