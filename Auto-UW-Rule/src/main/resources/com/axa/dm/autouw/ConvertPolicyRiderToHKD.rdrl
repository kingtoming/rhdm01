package com.axa.dm.autouw;

import java.lang.Number;

rule "ConvertPolicyRiderToHKD"
	dialect "mvel"
	when
		ExchangeRateObject( $fromCurrency : fromCurrency, toCurrency == "HKD" , $exchangeRate : exchangeRate)
		$policyRider : PolicyRider( sumInsuredHkd == null , $sumInsured : sumInsured, currency == $fromCurrency )
	then
		modify( $policyRider ) {
				setSumInsuredHkd( $sumInsured.multiply($exchangeRate) )
		}
end
